int firstUniqChar(string& s) {
   vector<int>Alphabet(26,0);
   for(int i=0;i<s.size();i++){
    Alphabet[s[i]-'a']++;
   }
   for(int i=0;i<s.size();i++){
    if(Alphabet[s[i]-'a']==1){
        return i;
    }
   }
    return -1;
}
