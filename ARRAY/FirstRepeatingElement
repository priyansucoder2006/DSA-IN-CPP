nt firstRepeatingElement(vector<int>& arr) {
    // Your code here
    for(int i=0;i<arr.size();i++){
        for(int j=i+1;j<arr.size();j++){
            if(arr[j]==arr[i]){
                return i+1;
            }
        }
    }
    return -1;
    
}
