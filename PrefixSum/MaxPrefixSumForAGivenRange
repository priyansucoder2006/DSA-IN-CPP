vector<int> maxPrefixes(vector<int>& arr, vector<int>& leftIndex, vector<int>& rightIndex) {
    // Your code here
    vector<int>answer;
    int n=leftIndex.size()-1;
    for(int i=0;i<=n;i++){
         int sum=0;
    int ans=INT_MIN;
        for(int j=leftIndex[i];j<=rightIndex[i];j++){
           sum+=arr[j];
            ans=max(ans,sum);
        }
        answer.push_back(ans);
    }
    return answer; 
}
